---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import AdSense from '../components/AdSense.astro';
import Recommend from '../components/Recommend.astro';

const pathname = Astro.url.pathname;
const locale = pathname.startsWith('/en/') || pathname === '/en'
  ? 'en'
  : pathname.startsWith('/ja/') || pathname === '/ja'
    ? 'ja'
    : 'ko';

const t = {
  ko: { title: '오늘의 일본어 | ManyTool', desc: '매일 1개의 일본어 표현을 뜻과 예문으로 학습합니다.', h1: '오늘의 일본어', sub: '실전 회화용 표현 1개씩', copy: '복사', copied: '복사됨' },
  en: { title: 'Daily Japanese | ManyTool', desc: 'Learn one Japanese expression each day with meaning and example.', h1: 'Daily Japanese', sub: 'One practical phrase every day', copy: 'Copy', copied: 'Copied' },
  ja: { title: '今日の日本語 | ManyTool', desc: '毎日1つの日本語表現を意味と例文で学びます。', h1: '今日の日本語', sub: '会話で使える表現を毎日1つ', copy: 'コピー', copied: 'コピー完了' }
}[locale];

---

<Layout title={t.title} description={t.desc}>
  <Header />
  <main class="mx-auto max-w-4xl px-4 py-8">
    <h1 class="text-3xl font-black">{t.h1}</h1>
    <p class="mt-2 text-slate-700">{t.sub}</p>
    <AdSense slotName="본문 상단" />
    <section class="mt-5 rounded-xl border border-slate-200 bg-white p-6">
      <h2 id="word" class="text-3xl font-black text-slate-900">-</h2>
      <p id="meaning" class="mt-2 text-lg font-semibold text-slate-800">-</p>
      <p id="example" class="mt-2 rounded-lg bg-slate-100 px-3 py-2 text-slate-700">-</p>
      <button id="copy-btn" class="mt-4 rounded-md border border-slate-300 bg-white px-3 py-2 text-sm font-semibold">{t.copy}</button>
    </section>
    <AdSense slotName="본문 하단" />
    <Recommend />
  </main>
  <Footer />
  <script is:inline define:vars={{ copiedText: t.copied }}>
    const words = [
      ['おはようございます', '좋은 아침입니다'],
      ['ありがとうございます', '감사합니다'],
      ['すみません', '실례합니다 / 죄송합니다'],
      ['大丈夫です', '괜찮습니다'],
      ['お願いします', '부탁합니다'],
      ['わかりました', '알겠습니다'],
      ['楽しいです', '재미있습니다'],
      ['また明日', '내일 또 봐요']
    ];
    const contexts = ['仕事で', '学校で', '旅行で', 'お店で', '会話で', '配信で', '練習で', '日常で'];
    const templates = ['%c% %w%。', '%w% を %c% 使いましょう。', '%c% %w% が役立ちます。', '今日は %c% %w%。'];
    const tones = ['ゆっくり', 'しっかり', '自然に', '丁寧に', '気軽に', '前向きに', '明るく', '実用的に', '短く', '毎日', '集中して', '楽しく', '柔らかく', '確実に', '一歩ずつ'];
    const entries = [];
    words.forEach(([w, m]) => {
      contexts.forEach((c) => {
        templates.forEach((tmp) => {
          tones.forEach((tone) => {
            entries.push({ word: w, meaning: m, ex: `${tmp.replace('%w%', w).replace('%c%', c)} ${tone}` });
          });
        });
      });
    });
    const date = new Date();
    const key = `${date.getFullYear()}-${date.getMonth() + 1}-${date.getDate()}`;
    let hash = 0;
    for (let i = 0; i < key.length; i += 1) hash = (hash * 29 + key.charCodeAt(i)) >>> 0;
    const pick = entries[hash % entries.length];
    const wordEl = document.getElementById('word');
    const meaningEl = document.getElementById('meaning');
    const exampleEl = document.getElementById('example');
    const copyBtn = document.getElementById('copy-btn');
    if (wordEl) wordEl.textContent = pick.word;
    if (meaningEl) meaningEl.textContent = pick.meaning;
    if (exampleEl) exampleEl.textContent = pick.ex;
    copyBtn?.addEventListener('click', async () => {
      await navigator.clipboard.writeText(`${pick.word} - ${pick.meaning} / ${pick.ex}`);
      if (copyBtn instanceof HTMLButtonElement) copyBtn.textContent = copiedText;
    });
  </script>
</Layout>
